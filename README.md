# Artelux — Site Institucional

Projeto front-end em React + Vite + Tailwind criado como protótipo para a Artelux.

Instalação:

```bash
npm install
npm run dev
```

Notas:
- Substitua as imagens em `src/assets` pelos arquivos reais.
- Dados principais em `src/data.js` para facilitar edições.
